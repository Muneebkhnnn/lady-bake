import CartPageClient from "@/components/CartPageClient";

export const metadata = {
  title: "Your Cart",
  description: "Review your order and send it to LadyBake on WhatsApp.",
};

export default function CartPage() {
  return <CartPageClient />;
}
